import React from 'react';
import Header from "../components/Header";

const Home = () => {

    return(
        <section className = "header">
            <nav>
              <Header />
            </nav>
            <h1>Home</h1>
        </section>
    );
};

export default Home;
